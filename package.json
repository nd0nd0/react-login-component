{
  "name": "vite-login",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter server start",
    "test": "pnpm --filter client test"
  }
}
